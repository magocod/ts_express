import path from "path";

export const tmpPath = path.resolve(__dirname, "../tmp");
export const resourcePath = path.resolve(__dirname, "../resources");

export const tmpPathUpload = path.join(tmpPath, "uploads")
export const tmpPathTxt = path.join(tmpPath, "txt")
